Q1. WHat is Kafka and how to we use it in our pipeline?

Q2. Difference between Spark Scheduler and Airflow schedule method

Q3. docker-compose Airflow has ```dockerfile: Dockerfile.airflow```, but postgres doesn't have

Q4. docker-compose spark master: ```entrypoint: [ "/opt/bitnami/spark/sbin/start-master.sh" ]``` ? 

Q5. Dockerfile.airflow ```RUN chmod -R 755 /airflow/app/logs```