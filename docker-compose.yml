services:
  # airflow:
  #   depends_on:
  #     - postgres
  #   build:
  #     context: .
  #     args:
  #       AIRFLOW_VERSION: "2.9.3"
  #       AIRFLOW_PYTHON_VERSION: "3.8"
  #     dockerfile: Dockerfile.airflow
  #   environment:
  #     AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
  #     AIRFLOW__CORE__DAGS_FOLDER: "/airflow/dags"
  #     AIRFLOW__CORE__PLUGINS_FOLDER: "/airflow/plugins"
  #     AIRFLOW__CORE__LOAD_EXAMPLES: "False"
  #     AIRFLOW__CORE__FERNET_KEY: "rRM_xazRD5Z0m7buczl11_dMgZZ_bJKF1UgKzrU9ax0="
  #     CONNECTION_CHECK_MAX_COUNT: "0"
  #     _AIRFLOW_DB_MIGRATE: 'true'
  #     _AIRFLOW_WWW_USER_CREATE: 'true'
  #     _AIRFLOW_WWW_USER_USERNAME: airflow
  #     _AIRFLOW_WWW_USER_PASSWORD: airflow

  #   volumes:
  #     - ./airflow:/airflow #锁定当前的local folder到container里的airflow(即disk里的位置)。在dockercompse的文件里，人为建立的WORKDIR /airflow
  #     - ./spark/data:/airflow/data
  #   ports:
  #     - "8080:8080"
  #   entrypoint: >
  #     bash -c '
  #       mkdir -p /airflow/app/logs
  #       export PYTHONPATH="/airflow/dags:$PYTHONPATH"
  #       airflow db init
  #       airflow users create --username ${_AIRFLOW_WWW_USER_USERNAME:-airflow} --firstname Admin --lastname User --role Admin --email admin@example.com --password ${_AIRFLOW_WWW_USER_PASSWORD:-airflow}
  #       exec supervisord -c /etc/supervisor/conf.d/supervisord.conf
  #     '

  # postgres:
  #   image: postgres:15.8
  #   restart: always
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: airflow
  #     POSTGRES_PASSWORD: airflow
  #   volumes:
  #     - ./postgres_data:/docker-entrypoint-initdb.d  # Mount custom SQL scripts here
  #   healthcheck:
  #     test: ["CMD", "pg_isready", "-U", "airflow"]
  #     interval: 30s
  #     retries: 3
  #     start_period: 20s
      
  # spark-master:
  #   build:
  #     context: . #build时lock一个path
  #     dockerfile: Dockerfile.spark
  #     args:
  #       SPARK_VERSION: "3.5.1"
  #   container_name: spark-master
  #   ports:
  #     - "8082:8080" # Spark UI
  #     - "7077:7077" # Spark Master port
  #   # extra_hosts:
  #   #   - "spark-master:172.23.0.3"
  #   environment:
  #     - SPARK_MODE=master
  #     - SPARK_MASTER_HOST=spark-master # Explicitly set Spark master host
  #     - SPARK_NO_DAEMONIZE=true
  #     - SPARK_DEPLOY_RECOVERY_MODE=NONE # Set this in the worker environment
  #   volumes:
  #     - ./spark/data:/data
  #     - ./spark/python_code:/usr/local/spark/python_code
  #   entrypoint: [ "/opt/bitnami/spark/sbin/start-master.sh" ]

  # spark-worker:
  #   image: bitnami/spark:3.5.1
  #   container_name: spark-worker
  #   depends_on:
  #     - spark-master
  #   environment:
  #     - SPARK_MODE=worker
  #     - SPARK_MASTER=spark://spark-master:7077
  #     - SPARK_WORKER_CORES=2
  #     - SPARK_WORKER_MEMORY=4G
  #     - SPARK_WORKER_LOG_DIR=/data/spark-logs
  #     - SPARK_DEPLOY_RECOVERY_MODE=NONE
  #   volumes:
  #     - ./spark/data:/data
  #     - ./spark/data/spark-logs:/data/spark-logs 
  #   ports:
  #     - "8081:8081"
  #   # extra_hosts:
  #   #   - "spark-master:172.23.0.3"

  # spark-history-server:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.spark
  #     args:
  #       SPARK_VERSION: "3.5.1"
  #   container_name: spark-history-server
  #   environment:
  #     - SPARK_HISTORY_OPTS=-Dspark.history.fs.logDirectory=/data/spark-logs
  #     - SPARK_NO_DAEMONIZE=true
  #   volumes:
  #     - ./spark/data/spark-logs:/data/spark-logs
  #   ports:
  #     - "18080:18080" # Spark History Server UI
  #   entrypoint: [ "/opt/bitnami/spark/sbin/start-history-server.sh" ]


  # zookeeper:
  #   image: confluentinc/cp-zookeeper:7.3.2
  #   container_name: zookeeper
  #   hostname: zoo
  #   #volumes:
  #   #  - "./zookeeper_data:/bitnami"
  #   ports:
  #     - "2181:2181"
  #   environment:
  #     ZOOKEEPER_CLIENT_PORT: 2181
  #     ZOOKEEPER_SERVER_ID: 1
  #     ZOOKEEPER_SERVERS: zoo:2888:3888
  #   #networks:
  #   #  - kafka-network

  # kafka:
  #   image: docker.io/bitnami/kafka:3.7
  #   container_name: kafka
  #   hostname: kafka1
  #   ports:
  #     - "9092:9092"
  #     - "9093:9093"
  #     - "19092:19092"
  #   volumes:
  #     - "kafka_data:/bitnami"
  #   environment:
  #     # KRaft settings
  #     - KAFKA_CFG_NODE_ID=0
  #     - KAFKA_CFG_PROCESS_ROLES=controller,broker
  #     - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093
  #     # Listeners
  #     - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093,DOCKER_HACK://:19092
  #     - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://:9092
  #     - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
  #     - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
  #     - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
  #   healthcheck:
  #     test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5
  #   #networks:
  #   #  - kafka-network

  zoo1:
    image: confluentinc/cp-zookeeper:7.3.2
    hostname: zoo1
    container_name: zoo1
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_SERVER_ID: 1
      ZOOKEEPER_SERVERS: zoo1:2888:3888 #2888 follower port 3888 election port

  kafka1:
    image: confluentinc/cp-kafka:7.3.2
    hostname: kafka1
    container_name: kafka1
    ports:
      - "9092:9092"
      - "29092:29092"
      - "9999:9999"
    environment:
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      # KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka1:19092,EXTERNAL://${DOCKER_HOST_IP:-127.0.0.1}:9092,DOCKER://host.docker.internal:29092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      # KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,DOCKER:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_ZOOKEEPER_CONNECT: "zoo1:2181"
      KAFKA_BROKER_ID: 1
      KAFKA_LOG4J_LOGGERS: "kafka.controller=INFO,kafka.producer.async.DefaultEventHandler=INFO,state.change.logger=INFO"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_JMX_PORT: 9999 # data of Kafka itself
      KAFKA_JMX_HOSTNAME: ${DOCKER_HOST_IP:-127.0.0.1}
      KAFKA_AUTHORIZER_CLASS_NAME: kafka.security.authorizer.AclAuthorizer
      KAFKA_ALLOW_EVERYONE_IF_NO_ACL_FOUND: "true"
    depends_on:
      - zoo1
    # command: sh -c "
    #   echo 'wait for Kafka to start';
    #   while ! nc -z kafka1:9092; do
    #     sleep 1; 
    #   done;
    #   kafka-topics --bootstrap-server kafka1:9092 --list;
    #   echo 'Creating Kafka topics';
    #   kafka-topics --bootstrap-server kafka1:9092 --create --if-not-exists --topic test --replication-factor 1 --partitions 2;
    #   echo 'Successfully created the following topics:';
    #   kafka-topics --bootstrap-server kafka1:9092 --list;
    #   "

  # init-kafka:
  #   image: confluentinc/cp-kafka:7.3.2
  #   depends_on:
  #   - kafka1
  #   entrypoint: [ '/bin/sh', '-c' ]
  #   command: |
  #     "
  #     # blocks until kafka is reachable
  #     kafka-topics --bootstrap-server kafka1:29092 --list
  #     echo -e 'Creating kafka topics'
  #     kafka-topics --bootstrap-server kafka1:29092 --create --if-not-exists --topic test --replication-factor 1 --partitions 1 # set up name of topic, and number of partitions
  #     echo -e 'Successfully created the following topics:'
  #     kafka-topics --bootstrap-server kafka1:29092 --list
  #    "
volumes:
  postgres_data:
  # kafka_data:
  #   driver: local